# 整页轮廓工作区开发报告

日期：2026-08-24

## 完成内容

- 元素分类、结构树分类和元素框坐标字段统一使用中文展示。
- 新增共享 `KIND_LABEL` 与 `KIND_COLOR`，六类元素使用六种独立颜色；可疑与选中状态保持更高优先级。
- 原图取消固定 `900px` 上限和左栏 `18px` 内边距，在普通缩放下横向无留白填满面板。
- 支持滚轮双向缩放，范围 `20%` 至 `400%`；缩放围绕指针锚定，并合并同一渲染周期的高频滚轮校正。
- 无项目首屏改为独立的约 `9:18` 手机预览上传界面，支持选择、拖放、格式提示、推荐尺寸、busy 状态和模型配置指引。
- 将旧画布中的字体选择、模型配置刷新、代码导出、整页比对、分析统计、待办提示、配置检查、上传后自动分析和分析失败重试迁移到当前工作流。
- 整页比对迁移为独立模态面板，保留生成、原图叠加与透明度调整能力。
- 删除 `canvas/` 目录、旧区域详情组件、旧区域编辑状态及相关死代码；项目加载后直接进入整页轮廓。

## 浏览器走查

环境：Chrome，前端 `http://localhost:5190`，API `http://127.0.0.1:4810`。

已验证：

- 首屏手机上传框在 `1440×620` 视口内完整显示：高度 `572px`，底部 `596px`，未溢出 `620px` 视口。
- 无项目和有项目页面均不再挂载 `.pipeline-canvas`。
- 已有项目直接展示 `94` 个元素的整页轮廓，页面舞台初始样式为 `width: 100%`。
- 结构树类型显示为“组件、图片、文字、网格、图标”等中文名称。
- 字体、导出、整页比对、模型配置刷新四项迁移工具均存在。
- 滚轮向上由 `100%` 缩放到 `110%`，反向恢复到 `100%`。
- 独立整页比对可打开，显示“生成整页比对”；按 `Escape` 后关闭。
- 浏览器控制台最终为 `0` 个错误、`0` 个警告。

## 自动验证

- 仓库 Vitest workspace：`21` 个测试文件、`93` 项测试全部通过。
- 前端测试：`9` 个测试文件、`39` 项测试全部通过。
- `vue-tsc --noEmit -p tsconfig.json` 通过。
- `pnpm -r build` 通过（核心包 `tsc` 与前端 `vite build` 均成功）。
- 未修改 `packages/region-split` API 源码，前端未引入新 API。
- 最终 Chrome 走查确认图像面板 `padding: 0px`、舞台 `width: 100%`、左右间隙 `0px`，且控制台 `0` 错误、`0` 警告。
