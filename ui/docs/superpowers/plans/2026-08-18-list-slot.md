# 列表与槽位 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让"同一父级下的一组平级元素共用统一宽高与间距"立得住——补上槽位尺寸、把 `kind: "grid"` 与 `repeat` 绑在一起、生成代码时塌成一条 `> *` 规则。

**Architecture:** 槽位是**纯几何**,由 `detectRepeat` 算出、随 `recomputeLayout` 重算,人工改过的不被冲掉。生成器不再给列表项各自写宽高,统一交给 `> *`。

**Tech Stack:** TypeScript、Vitest、zod、Vue 3。

设计依据:`docs/superpowers/specs/2026-08-17-list-slot-design.md`。

## 基线(动手前已核实,2026-08-18)

- 全量 **625 个测试通过**(core 359 / ui 266),两个包 typecheck 干净。
- `emit-html.ts` **已存在**并已处理重复组:出 `.e-xxx > *` 规则,宽度用 `pitch`,带 `!important`。本计划要去掉那个 `!important`(理由见 Task 3)。
- `repeat` 现为 `{ count, templateId, pitch }`,**没有槽位**。
- `setKind` 清 `borderRadius` 与 `textBox`,**不碰 `repeat`**。
- `recomputeLayout` 会 `delete next.repeat`,**从不改 `kind`**。
- 画布已是**每个区域一个独立详情节点**(`v-for="region in openedRegions"`),元素 store 由 `:create-element-store` 逐个创建;`CodeNode` 已挂载,连线已实现。**本计划不动画布结构。**

## Global Constraints

- 基准图 `packages/region-split/test-fixtures/maicai.png`,坐标一律**原图像素**。
- 阈值与取舍在注释里写明实测依据。测试写死实测数据,**不放宽断言**。
- `element-grid.ts` / `element-layout.ts` / `emit-html.ts` 必须**浏览器安全**:只能 `import type` 引 `RawImage`,不得引 sharp。
- 改了 `packages/region-split` 必须重启 API,tsx 不热重载。
- 命令在 `D:\workspace\ui` 下跑,用 `D:/nodejs/corepack.cmd pnpm`;单包测试先 `cd` 进包目录。
- **不放宽 `detectRepeat` 的"至少 3 个子块"门槛。** 实测 30 组双子元素容器全是「图标 + 文字」异构组合,放宽会整批误判成列表。

## 本轮不做

槽位偏离的告警标记(设计文档第 7 节:只有一个离群样本,阈值定不下来)、人工标记两项列表的入口、竖向列表的实机验证(现有 6 组正例全是横排)。

---

### Task 1: 槽位算出来并存下来

**Files:**
- Modify: `packages/region-split/src/element-grid.ts`
- Modify: `packages/region-split/src/element-grid.test.ts`
- Modify: `packages/region-split/src/element-types.ts`
- Modify: `packages/region-split/src/element-types.test.ts`
- Modify: `packages/region-split/src/element-detect.ts`
- Modify: `packages/region-split/src/element-layout.ts`

**Interfaces:**
- Consumes: 现有 `detectRepeat(boxes, direction): RepeatInfo | null`、`medianOf`
- Produces:
  - `RepeatInfo` 增加 `slot: { w: number; h: number }`
  - `ElementNode.repeat` 增加 `slot?: { w, h }` 与 `slotBy: "tool" | "human"`

- [ ] **Step 1: 写失败的测试**

追加到 `packages/region-split/src/element-grid.test.ts`:

```ts
describe("detectRepeat 的槽位", () => {
  // 实测「快捷功能菜单」×5：宽 142/141/103/103/141、高 132/134/172/129/134。
  // 槽位取中位数而不是最大值——172 是框切错了，取最大会把整行撑高。
  it("takes the median of the children, not the maximum", () => {
    const boxes = [
      { x: 74, y: 192, w: 142, h: 132 },
      { x: 293, y: 190, w: 141, h: 134 },
      { x: 512, y: 196, w: 103, h: 172 },
      { x: 731, y: 195, w: 103, h: 129 },
      { x: 950, y: 190, w: 141, h: 134 },
    ];
    expect(detectRepeat(boxes, "row")!.slot).toEqual({ w: 141, h: 134 });
  });

  it("rounds the slot to whole pixels", () => {
    const boxes = [
      { x: 0, y: 0, w: 50, h: 41 },
      { x: 100, y: 0, w: 51, h: 40 },
      { x: 200, y: 0, w: 52, h: 42 },
      { x: 300, y: 0, w: 51, h: 40 },
    ];
    const repeat = detectRepeat(boxes, "row")!;
    expect(Number.isInteger(repeat.slot.w)).toBe(true);
    expect(Number.isInteger(repeat.slot.h)).toBe(true);
  });
});
```

追加到 `packages/region-split/src/element-types.test.ts`:

```ts
describe("repeat 的槽位", () => {
  const listNode = (over: Record<string, unknown>) => elementNodeSchema.parse({
    id: "n1", parentId: null, box: { x: 0, y: 0, w: 10, h: 10 },
    kind: "grid", displayName: "列表", uniformity: 1, ...over,
  });

  it("keeps the slot and who set it", () => {
    const parsed = listNode({
      repeat: {
        count: 5, templateId: "c1", pitch: 219.75,
        slot: { w: 141, h: 134 }, slotBy: "human",
      },
    });
    expect(parsed.repeat?.slot).toEqual({ w: 141, h: 134 });
    expect(parsed.repeat?.slotBy).toBe("human");
  });

  // 老文件里的 repeat 没有这两个字段，读出来不能炸
  it("reads a legacy repeat without a slot", () => {
    const parsed = listNode({ repeat: { count: 5, templateId: "c1", pitch: 219.75 } });
    expect(parsed.repeat?.slot).toBeUndefined();
    expect(parsed.repeat?.slotBy).toBe("tool");
  });
});
```

若该文件顶部还没 import `elementNodeSchema`,补进已有的那行 import。

- [ ] **Step 2: 跑测试确认它失败**

```bash
cd packages/region-split && D:/nodejs/corepack.cmd pnpm exec vitest run src/element-grid.test.ts src/element-types.test.ts
```

Expected: FAIL,`repeat.slot` 是 `undefined`。

- [ ] **Step 3: 写实现**

`packages/region-split/src/element-grid.ts`,把 `RepeatInfo` 与 `detectRepeat` 改成:

```ts
export interface RepeatInfo {
  count: number;
  templateIndex: number;
  pitch: number;
  /** 槽位尺寸。子块的墨迹居中放进去，不拉伸。 */
  slot: { w: number; h: number };
}

export function detectRepeat(boxes: Rect[], direction: Direction): RepeatInfo | null {
  if (boxes.length < 3) return null;
  const pitches = pitchesOf(centersOf(boxes, direction));
  const pitch = medianOf(pitches);
  if (pitch <= 0) return null;
  if (!pitches.every(value => Math.abs(value - pitch) <= PITCH_TOLERANCE)) return null;
  // 模板取最大的那个子块：被截断的子块偏小，照它生成会漏内容
  const sizes = boxes.map(box => sizeOf(box, direction));
  return {
    count: boxes.length,
    templateIndex: sizes.indexOf(Math.max(...sizes)),
    pitch,
    // 槽位取中位数而不是最大值：实测「快捷功能菜单」×5 的子高是
    // 132/134/172/129/134，那个 172 是框切错了，取最大会把整行撑高。
    // 偏离中位数太多本身就是"这一项检测错了"的信号，与字号量化里
    // 用簇内离散度当质量指标是同一个用法。
    slot: {
      w: Math.round(medianOf(boxes.map(box => box.w))),
      h: Math.round(medianOf(boxes.map(box => box.h))),
    },
  };
}
```

`packages/region-split/src/element-types.ts`,把 `repeat` 那段改成:

```ts
  repeat: z.object({
    count: z.number().int().min(2),
    templateId: z.string().min(1),
    /** 主轴上相邻项的中心距 */
    pitch: z.number(),
    /**
     * 槽位尺寸。子元素的墨迹居中放进去，不拉伸——三个图标量出
     * 56/52/54 不是误差，是它们本来就画得不一样大。
     * 老文件里没有这个字段，所以可选。
     */
    slot: z.object({
      w: z.number().int().nonnegative(),
      h: z.number().int().nonnegative(),
    }).optional(),
    /** 槽位是谁定的。human 的不被几何重算冲掉。 */
    slotBy: z.enum(["tool", "human"]).default("tool"),
  }).optional(),
```

`packages/region-split/src/element-detect.ts`,写 `parent.repeat` 那句补上槽位:

```ts
    parent.repeat = {
      count: repeat.count, templateId: template.id, pitch: repeat.pitch,
      slot: repeat.slot, slotBy: "tool",
    };
```

`packages/region-split/src/element-layout.ts` 的 `recomputeLayout` 里,写 `next.repeat` 那句同样补上,**并保留人工设过的槽位**:

```ts
    if (repeat && template) {
      const human = node.repeat?.slotBy === "human" ? node.repeat.slot : undefined;
      next.repeat = {
        count: repeat.count, templateId: template.id, pitch: repeat.pitch,
        slot: human ?? repeat.slot,
        slotBy: human ? "human" : "tool",
      };
    } else {
      delete next.repeat;
    }
```

槽位的来历记在节点自己身上(`slotBy`),不用像 `scrollOverrides` 那样另起一个集合——`scrollX/Y` 是裸布尔值、没有出处可记,槽位有。

- [ ] **Step 4: 跑测试确认通过**

```bash
cd packages/region-split && D:/nodejs/corepack.cmd pnpm test && D:/nodejs/corepack.cmd pnpm exec tsc --noEmit -p tsconfig.json
```

Expected: 全部 PASS,typecheck 无输出。若 `element-detect.test.ts` 或 `element-layout.test.ts` 里有断言写死了不含 `slot` 的 `repeat` 对象,**按实测值补上 `slot` 与 `slotBy`,不要改成 `toMatchObject` 之类放宽比较**。

- [ ] **Step 5: 提交**

```bash
git add packages/region-split/src/element-grid.ts packages/region-split/src/element-grid.test.ts packages/region-split/src/element-types.ts packages/region-split/src/element-types.test.ts packages/region-split/src/element-detect.ts packages/region-split/src/element-layout.ts
git commit -m "feat: record the slot size of a repeat group"
```

---

### Task 2: 把 `kind: "grid"` 与 `repeat` 绑在一起

**Files:**
- Modify: `packages/region-split/src/element-layout.ts`
- Modify: `packages/region-split/src/element-layout.test.ts`
- Modify: `apps/region-split-ui/src/element-state.ts`
- Modify: `apps/region-split-ui/src/element-state.test.ts`

**Interfaces:**
- Consumes: Task 1 的 `repeat` 形状
- Produces: 两个字段不再脱节

- [ ] **Step 1: 写失败的测试**

追加到 `packages/region-split/src/element-layout.test.ts`:

```ts
describe("grid 与 repeat 的同步", () => {
  const container = (over: Partial<ElementNode> = {}): ElementNode => ({
    id: "g", parentId: null, box: { x: 0, y: 0, w: 300, h: 100 }, kind: "grid",
    displayName: "列表", style: {}, uniformity: 1, source: "auto",
    classification: "tool", scrollX: false, scrollY: false, positioning: "flow",
    repeat: { count: 3, templateId: "a", pitch: 100, slot: { w: 40, h: 40 }, slotBy: "tool" },
    ...over,
  });
  const child = (id: string, x: number): ElementNode => ({
    id, parentId: "g", box: { x, y: 30, w: 40, h: 40 }, kind: "component",
    displayName: id, style: {}, uniformity: 1, source: "auto",
    classification: "tool", scrollX: false, scrollY: false, positioning: "flow",
  });

  it("keeps the grid kind while the repeat still holds", () => {
    const out = recomputeLayout([container(), child("a", 10), child("b", 110), child("c", 210)]);
    expect(out[0]!.kind).toBe("grid");
    expect(out[0]!.repeat).toBeDefined();
  });

  // 人工挪乱了间距，repeat 被删——kind 不能还叫"网格"
  it("demotes the kind when the repeat no longer holds", () => {
    const out = recomputeLayout([container(), child("a", 10), child("b", 60), child("c", 210)]);
    expect(out[0]!.repeat).toBeUndefined();
    expect(out[0]!.kind).toBe("component");
  });

  // 人工定的类型不许被几何推翻
  it("leaves a human-set kind alone", () => {
    const out = recomputeLayout([
      container({ classification: "human" }),
      child("a", 10), child("b", 60), child("c", 210),
    ]);
    expect(out[0]!.repeat).toBeUndefined();
    expect(out[0]!.kind).toBe("grid");
  });

  it("keeps a human slot through a recompute", () => {
    const out = recomputeLayout([
      container({
        repeat: { count: 3, templateId: "a", pitch: 100, slot: { w: 60, h: 60 }, slotBy: "human" },
      }),
      child("a", 10), child("b", 110), child("c", 210),
    ]);
    expect(out[0]!.repeat?.slot).toEqual({ w: 60, h: 60 });
    expect(out[0]!.repeat?.slotBy).toBe("human");
  });
});
```

追加到 `apps/region-split-ui/src/element-state.test.ts`(沿用该文件已有的 `createElementStore` / `loaded` / `node` helper):

```ts
  const listTree = () => loaded([node({
    id: "n1", kind: "grid",
    repeat: { count: 3, templateId: "n2", pitch: 100, slot: { w: 40, h: 40 }, slotBy: "tool" },
  })]);
  const whole = { x: 0, y: 0, w: 100, h: 100 };

  it("drops the repeat when the kind moves away from grid", async () => {
    const store = createElementStore(listTree());
    await store.load("p1", whole);
    await store.setKind("p1", whole, "n1", "component");
    expect(store.nodes.value[0]!.repeat).toBeUndefined();
  });

  it("keeps the repeat when the kind stays grid", async () => {
    const store = createElementStore(listTree());
    await store.load("p1", whole);
    await store.setKind("p1", whole, "n1", "grid");
    expect(store.nodes.value[0]!.repeat).toBeDefined();
  });
```

- [ ] **Step 2: 跑测试确认它失败**

```bash
cd packages/region-split && D:/nodejs/corepack.cmd pnpm exec vitest run src/element-layout.test.ts
```

```bash
cd apps/region-split-ui && D:/nodejs/corepack.cmd pnpm exec vitest run src/element-state.test.ts
```

Expected: 两边都 FAIL——`kind` 仍是 `grid`;`repeat` 仍在。

- [ ] **Step 3: 写实现**

`packages/region-split/src/element-layout.ts`,把 `delete next.repeat` 那个分支改成:

```ts
    } else {
      delete next.repeat;
      // 等距不成立了就不该再叫"网格"——kind 与 repeat 是检测时同一个 if 里赋的，
      // 只删一个会留下一个没有重复信息的 grid。人工定的类型不许被几何推翻。
      if (next.kind === "grid" && next.classification !== "human") next.kind = "component";
    }
```

`apps/region-split-ui/src/element-state.ts`,照 `dropTextBox` 的写法加:

```ts
/** 不再是网格了，重复信息就成了孤儿——清掉，别留下自相矛盾的数据 */
function dropRepeat(node: ElementNode): ElementNode {
  if (node.repeat === undefined) return node;
  const { repeat: _drop, ...rest } = node;
  return rest;
}
```

并在 `setKind` 里用上:

```ts
    async setKind(projectId: string, region: Rect, id: string, kind: ElementKind) {
      await commit(projectId, region, nodes.value.map(node => {
        if (node.id !== id) return node;
        const style = { ...node.style };
        if (!supportsBorderRadius(kind)) delete style.borderRadius;
        const next = dropTextBox({ ...node, kind, style, classification: "human" as const });
        return kind === "grid" ? next : dropRepeat(next);
      }));
    },
```

- [ ] **Step 4: 跑测试确认通过**

```bash
cd packages/region-split && D:/nodejs/corepack.cmd pnpm test && D:/nodejs/corepack.cmd pnpm exec tsc --noEmit -p tsconfig.json
```

```bash
cd apps/region-split-ui && D:/nodejs/corepack.cmd pnpm test && D:/nodejs/corepack.cmd pnpm exec vue-tsc --noEmit -p tsconfig.json
```

Expected: 全部 PASS。

- [ ] **Step 5: 提交**

```bash
git add packages/region-split/src/element-layout.ts packages/region-split/src/element-layout.test.ts apps/region-split-ui/src/element-state.ts apps/region-split-ui/src/element-state.test.ts
git commit -m "fix: keep the grid kind and the repeat from drifting apart"
```

---

### Task 3: 生成器用槽位,并去掉 `!important`

**Files:**
- Modify: `packages/region-split/src/emit-html.ts`
- Modify: `packages/region-split/src/emit-html.test.ts`

**Interfaces:**
- Consumes: Task 1 的 `repeat.slot`
- Produces: 列表项不再各自写宽高;`> *` 规则同时给主轴与交叉轴尺寸

**动手前先通读一遍现有的 `emitHtml`**,下面只说要改的三处。

- [ ] **Step 1: 写失败的测试**

追加到 `packages/region-split/src/emit-html.test.ts`(沿用该文件已有的 `node` / `emit` helper):

```ts
describe("列表槽位", () => {
  /** 实测「快捷功能菜单」：pitch 219.75，槽位中位数 141×134 */
  const grid = () => [
    node({
      id: "g", kind: "grid", box: { x: 36, y: 100, w: 1098, h: 255 },
      layout: { direction: "row", gap: 98, padding: { top: 59, right: 42, bottom: 18, left: 38 } },
      repeat: { count: 5, templateId: "c1", pitch: 219.75, slot: { w: 141, h: 134 }, slotBy: "tool" },
    }),
    node({ id: "c1", parentId: "g", box: { x: 74, y: 192, w: 142, h: 132 } }),
    node({ id: "c2", parentId: "g", box: { x: 293, y: 190, w: 141, h: 134 } }),
    node({ id: "c3", parentId: "g", box: { x: 512, y: 196, w: 103, h: 172 } }),
  ];

  // 主轴用 pitch（那才是让项等距排开的量），交叉轴用槽位
  it("sizes the cells from the pitch and the slot", () => {
    const rule = /\.e-g > \* \{([^}]*)\}/.exec(emit(grid()).css)![1]!;
    expect(rule).toContain("width: 18.7821vw");   // 219.75 / 1170
    expect(rule).toContain("height: 11.453vw");   // 134 / 1170
  });

  // 尺寸由 > * 一条规则给，项自己不再写——这才是 ul > li 该有的样子
  it("does not repeat the size on every item", () => {
    const item = /\.e-c1 \{([^}]*)\}/.exec(emit(grid()).css)![1]!;
    expect(item).not.toContain("width:");
    expect(item).not.toContain("height:");
  });

  // 项自己不写宽高之后，就不需要用 !important 去压它了
  it("needs no important flag", () => {
    expect(emit(grid()).css).not.toContain("!important");
  });

  it("swaps the axes for a column list", () => {
    const rule = /\.e-g > \* \{([^}]*)\}/.exec(emit([
      node({
        id: "g", kind: "grid", box: { x: 0, y: 100, w: 200, h: 600 },
        layout: { direction: "column", gap: 20, padding: { top: 0, right: 0, bottom: 0, left: 0 } },
        repeat: { count: 3, templateId: "c1", pitch: 200, slot: { w: 180, h: 180 }, slotBy: "tool" },
      }),
      node({ id: "c1", parentId: "g", box: { x: 10, y: 110, w: 180, h: 180 } }),
      node({ id: "c2", parentId: "g", box: { x: 10, y: 310, w: 180, h: 180 } }),
      node({ id: "c3", parentId: "g", box: { x: 10, y: 510, w: 180, h: 180 } }),
    ]).css)![1]!;
    expect(rule).toContain("height: 17.094vw");   // pitch 200
    expect(rule).toContain("width: 15.3846vw");   // slot.w 180
  });

  // 老数据没有槽位：退回只给主轴，别崩
  it("falls back to the pitch alone when there is no slot", () => {
    const rule = /\.e-g > \* \{([^}]*)\}/.exec(emit([
      node({
        id: "g", kind: "grid", box: { x: 0, y: 100, w: 300, h: 100 },
        layout: { direction: "row", gap: 10, padding: { top: 0, right: 0, bottom: 0, left: 0 } },
        repeat: { count: 3, templateId: "c1", pitch: 100, slotBy: "tool" },
      }),
      node({ id: "c1", parentId: "g", box: { x: 0, y: 100, w: 90, h: 100 } }),
      node({ id: "c2", parentId: "g", box: { x: 100, y: 100, w: 90, h: 100 } }),
      node({ id: "c3", parentId: "g", box: { x: 200, y: 100, w: 90, h: 100 } }),
    ]).css)![1]!;
    expect(rule).toContain("width: 8.547vw");
    expect(rule).not.toContain("height:");
  });
});
```

- [ ] **Step 2: 跑测试确认它失败**

```bash
cd packages/region-split && D:/nodejs/corepack.cmd pnpm exec vitest run src/emit-html.test.ts
```

Expected: FAIL——`> *` 规则里没有 `height`,项自己仍写着宽高,`!important` 仍在。

- [ ] **Step 3: 写实现**

**其一**,遍历节点写规则时,列表项不再写宽高。函数开头先建一张父节点索引(不要在循环里反复 `find`):

```ts
  const byId = new Map(input.tree.nodes.map(item => [item.id, item]));
```

再把写 `width` / `height` 那两句包起来:

```ts
    // 列表项的尺寸由父节点的 `> *` 一条规则统一给出。项自己再写一遍，
    // 既是重复，也会因为选择器权重相同、后写的赢，逼得 `> *` 去用 !important。
    const inRepeat = node.parentId !== null
      && byId.get(node.parentId)?.repeat !== undefined;
    if (!inRepeat) {
      lines.push(`  width: ${toVw(node.box.w, designWidth)};`);
      lines.push(`  height: ${toVw(node.box.h, designWidth)};`);
    }
```

**其二**,`> *` 规则改成按方向分主轴/交叉轴,并去掉 `!important`:

```ts
    if (node.repeat) {
      const horizontal = node.layout?.direction !== "column";
      const main = `${horizontal ? "width" : "height"}: `
        + `${toVw(node.repeat.pitch, designWidth)};`;
      // 老文件的 repeat 没有槽位，那就只给主轴，交叉轴由内容撑
      const slot = node.repeat.slot;
      const cross = slot
        ? [`  ${horizontal ? "height" : "width"}: `
           + `${toVw(horizontal ? slot.h : slot.w, designWidth)};`]
        : [];
      cssBlocks.push([
        `/* ${node.displayName}：${node.repeat.count} 项重复。`,
        "   主轴用中心距、交叉轴用槽位；子块宽度不同，用 gap 会让位置沿主轴累积偏移。",
        "   墨迹居中放进槽位，不拉伸——三个图标量出 56/52/54 不是误差，",
        "   是它们本来就画得不一样大。 */",
        `.e-${classKey(node.id)} > * {`,
        `  ${main}`,
        ...cross,
        "  display: flex;",
        "  justify-content: center;",
        "  align-items: center;",
        "  flex: 0 0 auto;",
        "}",
      ].join("\n"));
    }
```

**其三**,若 `input.tree.nodes` 在该函数里已有别的局部名字,用真实的名字建 `byId`,不要新引入一份数据。

- [ ] **Step 4: 跑测试确认通过**

```bash
cd packages/region-split && D:/nodejs/corepack.cmd pnpm test && D:/nodejs/corepack.cmd pnpm exec tsc --noEmit -p tsconfig.json
```

Expected: 全部 PASS。若现有 emitter 测试里有断言依赖"每个节点都写了宽高",按新行为改断言并在该测试上方注明"列表项的尺寸改由 `> *` 统一给出"。

- [ ] **Step 5: 提交**

```bash
git add packages/region-split/src/emit-html.ts packages/region-split/src/emit-html.test.ts
git commit -m "feat: size list items from one rule instead of one per item"
```

---

### Task 4: 界面上把它呈现成"列表"

**Files:**
- Modify: `apps/region-split-ui/src/element-state.ts`
- Modify: `apps/region-split-ui/src/element-state.test.ts`
- Modify: `apps/region-split-ui/src/components/ElementTree.vue`
- Modify: `apps/region-split-ui/src/components/ElementTree.test.ts`
- Modify: `apps/region-split-ui/src/components/ElementProperties.vue`
- Modify: `apps/region-split-ui/src/components/ElementProperties.test.ts`
- Modify: `apps/region-split-ui/src/canvas/nodes/DetailNode.vue`

**Interfaces:**
- Consumes: Task 1 的 `repeat.slot` / `slotBy`
- Produces: `elementStore.setSlot(projectId, region, id, w, h)`

- [ ] **Step 1: 写失败的测试**

追加到 `apps/region-split-ui/src/element-state.test.ts`:

```ts
  it("sets the slot and marks it human", async () => {
    const store = createElementStore(listTree());
    await store.load("p1", whole);
    await store.setSlot("p1", whole, "n1", 60, 60);
    expect(store.nodes.value[0]!.repeat?.slot).toEqual({ w: 60, h: 60 });
    expect(store.nodes.value[0]!.repeat?.slotBy).toBe("human");
  });

  // 没有 repeat 的节点谈不上槽位，静默不动而不是造一个出来
  it("ignores a slot edit on a node with no repeat", async () => {
    const store = createElementStore(loaded([node({ id: "n1" })]));
    await store.load("p1", whole);
    await store.setSlot("p1", whole, "n1", 60, 60);
    expect(store.nodes.value[0]!.repeat).toBeUndefined();
  });
```

(`listTree` 与 `whole` 是 Task 2 里加的 helper,直接复用。)

追加到 `apps/region-split-ui/src/components/ElementTree.test.ts`:

```ts
  it("calls a repeat container a list with its count", () => {
    const wrapper = mountTree({
      nodes: [node({
        id: "n1", kind: "grid", displayName: "快捷功能菜单",
        repeat: { count: 5, templateId: "n2", pitch: 219.75, slot: { w: 141, h: 134 }, slotBy: "tool" },
      })],
    });
    expect(wrapper.find('[data-test="list-badge"]').text()).toBe("列表 ×5");
  });

  it("leaves an ordinary container alone", () => {
    expect(mountTree({ nodes: [node({ id: "n1", kind: "component" })] })
      .find('[data-test="list-badge"]').exists()).toBe(false);
  });
```

追加到 `apps/region-split-ui/src/components/ElementProperties.test.ts`(沿用顶层的 `const node`):

```ts
  const listNode = (over: Partial<ElementNode> = {}): ElementNode => ({
    ...node, kind: "grid", displayName: "快捷功能菜单",
    repeat: { count: 5, templateId: "c1", pitch: 219.75, slot: { w: 141, h: 134 }, slotBy: "tool" },
    ...over,
  });

  it("shows the slot and the pitch for a list", () => {
    const wrapper = mount(ElementProperties, { props: { node: listNode() } });
    expect((wrapper.find('[data-test="slot-w"]').element as HTMLInputElement).value).toBe("141");
    expect((wrapper.find('[data-test="slot-h"]').element as HTMLInputElement).value).toBe("134");
    expect(wrapper.find('[data-test="list-pitch"]').text()).toContain("219.8");
  });

  it("emits set-slot when the slot changes", async () => {
    const wrapper = mount(ElementProperties, { props: { node: listNode() } });
    const input = wrapper.find('[data-test="slot-w"]');
    await input.setValue("150");
    await input.trigger("change");
    expect(wrapper.emitted("set-slot")![0]).toEqual(["n1", 150, 134]);
  });

  it("hides the slot row for a node that is not a list", () => {
    expect(mount(ElementProperties, { props: { node } })
      .find('[data-test="slot-w"]').exists()).toBe(false);
  });
```

- [ ] **Step 2: 跑测试确认它失败**

```bash
cd apps/region-split-ui && D:/nodejs/corepack.cmd pnpm exec vitest run src/element-state.test.ts src/components/ElementTree.test.ts src/components/ElementProperties.test.ts
```

Expected: FAIL,`setSlot is not a function`、找不到 `[data-test="list-badge"]` 与 `[data-test="slot-w"]`。

- [ ] **Step 3: 写实现**

`apps/region-split-ui/src/element-state.ts`,照 `setRadius` 的写法加:

```ts
    /**
     * 人工改槽位。**不动任何子元素的 box**——槽位只管布局，
     * 墨迹框始终是测量结果，拉齐它们会把图标拉变形。
     */
    async setSlot(
      projectId: string, region: Rect, id: string, w: number, h: number,
    ) {
      const current = nodes.value.find(node => node.id === id);
      if (!current?.repeat) return;
      const slot = { w: Math.max(0, Math.round(w)), h: Math.max(0, Math.round(h)) };
      if (current.repeat.slot?.w === slot.w && current.repeat.slot?.h === slot.h) return;
      await commit(projectId, region, nodes.value.map(node => node.id === id
        ? { ...node, repeat: { ...node.repeat!, slot, slotBy: "human" as const } }
        : node));
    },
```

`ElementTree.vue`,在节点行里(与已有的类型角标同级)加:

```vue
        <span v-if="node.repeat" data-test="list-badge" class="list-badge">
          列表 ×{{ node.repeat.count }}
        </span>
```

样式照该文件已有角标的写法加一条 `.list-badge`。

`ElementProperties.vue`,在"重复"那一行附近加槽位与间距(照该文件已有 `field` 行的结构):

```vue
      <div v-if="props.node.repeat" class="field">
        <span class="name">槽位</span>
        <div class="axes">
          <label>W<input
            data-test="slot-w" type="number" :value="props.node.repeat.slot?.w ?? ''"
            :disabled="props.disabled" @change="onSlot($event, 'w')"
          ></label>
          <label>H<input
            data-test="slot-h" type="number" :value="props.node.repeat.slot?.h ?? ''"
            :disabled="props.disabled" @change="onSlot($event, 'h')"
          ></label>
        </div>
      </div>
      <div v-if="props.node.repeat" class="field">
        <span class="name">间距</span>
        <span data-test="list-pitch" class="value">
          {{ props.node.repeat.pitch.toFixed(1) }}
        </span>
      </div>
```

`<script setup>` 的 emits 里加:

```ts
  "set-slot": [id: string, w: number, h: number];
```

以及处理函数:

```ts
/** 槽位改一个轴，另一个轴带着当前值一起发出去，避免半个值落盘 */
function onSlot(event: Event, axis: "w" | "h") {
  const repeat = props.node?.repeat;
  if (!props.node || !repeat) return;
  const value = Number((event.target as HTMLInputElement).value);
  if (!Number.isFinite(value)) return;
  const slot = repeat.slot ?? { w: 0, h: 0 };
  emit("set-slot", props.node.id,
    axis === "w" ? value : slot.w,
    axis === "h" ? value : slot.h);
}
```

最后在 `DetailNode.vue` 里把这个事件接到 store 上,照它接 `@set-radius` 的写法加 `@set-slot`。

- [ ] **Step 4: 跑测试确认通过**

```bash
cd apps/region-split-ui && D:/nodejs/corepack.cmd pnpm test && D:/nodejs/corepack.cmd pnpm exec vue-tsc --noEmit -p tsconfig.json && D:/nodejs/corepack.cmd pnpm build
```

Expected: 全部 PASS,typecheck 无输出,build 成功。

- [ ] **Step 5: 在真浏览器里走一遍**

**改了核心包必须重启 API。** 起两个服务后打开 `http://localhost:5180`,上传 `packages/region-split/test-fixtures/maicai.png`,等分析完成,选中「常用服务」那类区域点"解析元素"。

确认:

1. 结构树里那个网格节点显示 **`列表 ×5`**;
2. 选中它,属性面板有**槽位 W/H** 和**间距**,槽位可改;
3. 改槽位之后,子元素的位置尺寸**不变**——槽位只管生成,不动测量结果;
4. 到「代码产出」点生成,CSS 里那条 `.e-xxx > *` **同时有 width 和 height、没有 `!important`**,而各个子项的规则里**不再有 width/height**;
5. 叠加比对里那一排格子,和改动前比是否更齐。

**把第 5 点的观察写进报告**——这是这轮改动值不值的判据。

- [ ] **Step 6: 提交**

```bash
git add apps/region-split-ui/src/components/ElementTree.vue apps/region-split-ui/src/components/ElementTree.test.ts apps/region-split-ui/src/components/ElementProperties.vue apps/region-split-ui/src/components/ElementProperties.test.ts apps/region-split-ui/src/element-state.ts apps/region-split-ui/src/element-state.test.ts apps/region-split-ui/src/canvas/nodes/DetailNode.vue
git commit -m "feat: show and edit the list slot"
```

---

## 完成标准

- `pnpm -r test` 全绿(基线 625,本计划会增加),`pnpm -r typecheck` 无输出,前端 `pnpm build` 成功。
- 生成的 CSS 里 `.e-xxx > *` 同时给出主轴与交叉轴尺寸、**没有 `!important`**;列表项自己的规则里没有 width/height。
- 人工把 grid 改成别的类型后 `repeat` 一并消失;人工挪乱间距后 `kind` 降回 `component`(人工定过类型的除外)。
- 走查报告写明改动前后那排格子的对齐差别。
