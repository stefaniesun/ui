import { describe,expect,it } from 'vitest'
import { runRefinement } from './run.js'

const quality=(total:number)=>({total,regions:[{regionId:'hero',total,critical:true,geometryErrorPx:2,ocrMatch:.99,severeDefects:[]}],code:{types:true,lint:true,componentContract:true}})
describe('runRefinement',()=>{it('runs phases, accepts a patch, and restores the best snapshot',async()=>{const snapshots:string[]=[],restored:string[]=[];let file='before';const result=await runRefinement({workspace:{async createSnapshot(label){snapshots.push(label);return label},async restoreSnapshot(id){restored.push(id)},async write(_path,content){file=content}},boundary:{bindings:[{regionId:'hero',componentPath:'src/Hero.vue',componentName:'Hero'}],stateIds:['default'],targets:['h5']},async ingest(){},async analyze(){},async generate(){},async render(){},async measure(){return quality(.86)},async diagnose(){return{plan:{targetRegionIds:['hero'],rootCause:'spacing',allowedFiles:['src/Hero.vue'],allowedComponents:['Hero'],allowedTokens:[],expectedMetricChanges:{total:.03},affectedStateIds:['default'],affectedTargets:['h5'],rollbackConditions:['regression']},payload:{'src/Hero.vue':'after'}}},async regress(){return quality(.89)}},8);expect(result.reason).toBe('passed');expect(result.bestRound).toBe(1);expect(file).toBe('after');expect(restored).toContain('round-1');expect(result.journal.filter(e=>e.status==='completed').map(e=>e.state)).toEqual(['INGEST','ANALYZE','GENERATE','RENDER','MEASURE','DIAGNOSE','PATCH','REGRESSION'])})})
