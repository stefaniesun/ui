import { mkdir,writeFile } from 'node:fs/promises'
import path from 'node:path'
export async function initPage(name:string,cwd=process.cwd()){if(!/^[a-z][a-z0-9-]*$/u.test(name))throw new Error('Invalid page name');const root=path.join(cwd,'fixtures',name);await mkdir(path.join(root,'reference'),{recursive:true});await writeFile(path.join(root,'manifest.yaml'),`version: 1.0.0\nprojectId: demo\npageId: ${name}\ndevice:\n  width: 396\n  height: 842\n  pixelRatio: 1\nstates:\n  - id: default\n    screenshot: reference/default.png\n    screenshotType: viewport\n    scale: 1\n`,{flag:'wx'}).catch(e=>{if((e as NodeJS.ErrnoException).code!=='EEXIST')throw e});await writeFile(path.join(root,'region-overrides.json'),'{}\n',{flag:'wx'}).catch(e=>{if((e as NodeJS.ErrnoException).code!=='EEXIST')throw e});return root}
