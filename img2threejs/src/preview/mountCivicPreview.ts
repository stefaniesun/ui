export function mountCivicPreview(host: HTMLElement): void {
  host.innerHTML = `
    <section class="shell">
      <h1>Loading Civic preview</h1>
      <p>Spec-driven Three.js model wiring starts in the next task.</p>
    </section>
  `;
}
